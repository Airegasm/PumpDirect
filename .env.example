# Public-side listener (Cloudflare tunnel terminates here).
PUBLIC_PORT=3000
PUBLIC_HOST=127.0.0.1

# Owner-side console (browser-only, loopback).
OWNER_PORT=3001
OWNER_HOST=127.0.0.1

AUTH_HEADER=Cf-Access-Authenticated-User-Email
TRUST_PROXY=loopback
LOG_LEVEL=INFO
NODE_ENV=production
